{
  "name": "defi-staking",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy:contracts:localhost": "npx hardhat deploy --network localhost --tags $npm_config_tags",
    "deploy:contracts:hardhat": "npx hardhat deploy --network hardhat --tags $npm_config_tags",
    "deploy:contracts:goerli": "npx hardhat deploy --network goerli --tags $npm_config_tags",
    "compile": "hardhat compile",
    "format": "npx prettier --write \"**/*.js\" \"**/*.sol\"",
    "lint": "eslint . --ext .js && solhint 'contracts/**/*.sol'",
    "test": "hardhat test",
    "coverage": "hardhat coverage"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Pere970/defi-staking.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Pere970/defi-staking/issues"
  },
  "homepage": "https://github.com/Pere970/defi-staking#readme",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "@nomiclabs/hardhat-ethers": "^2.1.1",
    "@typechain/ethers-v5": "^10.1.0",
    "@typechain/hardhat": "^6.1.3",
    "dotenv": "^16.0.2",
    "ethers": "^5.7.1",
    "hardhat": "^2.11.2",
    "hardhat-deploy": "^0.11.10",
    "hardhat-deploy-ethers": "^0.3.0-beta.13",
    "hardhat-deploy-tenderly": "^0.2.0",
    "hardhat-gas-reporter": "^1.0.9",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.7.3"
  }
}
